pipeline {
    agent any
    tools{
        maven 'Maven 3.9'
    }

    stages {
        stage('build') {
            echo 'Building the project'
        }

        stage('test') {
            echo 'Testing the project'
        }

        stage('deploy') {
            echo 'Deploying the project'
        }
    }


}